# Example environment variables for TIM3 local/dev usage
# Copy to .env.local and adjust as needed. Do not commit real secrets.

# Optional: Path to AOS binary if not on PATH
# AOS_BIN=/usr/local/bin/aos

# Optional: AO/AOS endpoint (if your aos uses a remote endpoint)
# AOS_URL=http://localhost:1984

# Optional: Wallet path or JWK for local testing
# WALLET_PATH=~/.config/ao/test-wallet.json
