-- TIM3 Coordinator (Orchestrator)
-- In AOS: create/select a process, then run:
.load ao/coordinator/build/process.lua

print('Coordinator loaded. Use Configure messages to set Lock/Token/State/USDA PIDs.')

print("PID: " .. ao.id)
print("Copy this PID into: configure-integration.lua, verify-e2e.lua, verify-test-processes.lua")
